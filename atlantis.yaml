version: 3

projects:
  - name: terraform-ansible-baseline
    dir: terraform
    workflow: default
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "../atlantis.yaml"
    apply_requirements:
      - approved
      - mergeable
