version: 3

projects:
  - name: terraform-ansible-baseline
    dir: terraform
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "../atlantis.yaml"
